//! ## fljúga handahófi MLIR structured control flow dialect support.
//!
//! Implements mlir structured control flow dialect - `if`/`for` loops and conds are structured.
//! `goto`/`assert` are unstructured.

